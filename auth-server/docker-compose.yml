services:
  db:
    image: mysql:8.0
    container_name: zeti-mysql
    # 아래 command 부분을 추가
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: zeti_db
    volumes:
      - ./mysql_data:/var/lib/mysql