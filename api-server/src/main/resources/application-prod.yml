spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: false
  sql:
    init:
      mode: never

logging:
  level:
    root: INFO
    com.zeti.api: INFO
